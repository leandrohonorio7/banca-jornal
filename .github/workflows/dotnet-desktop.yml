# Workflow desativado - Este projeto foi migrado de WPF Desktop para Web (Blazor WASM + ASP.NET Core API)
# Use o workflow deploy-hostinger.yml para CI/CD

# Este arquivo é mantido apenas para histórico
# Criado originalmente para WPF, mas o projeto agora é uma aplicação web containerizada
# Deploy automático configurado em: .github/workflows/deploy-hostinger.yml
